get:
  summary: Returns a list of supported datasource drivers
  operationId: drivers
  responses:
    200:
      description: A list of supported datasource drivers
      content:
        'application/json':
          schema:
            type: array
            items:
              $ref: '../schemas/driver.yaml'
