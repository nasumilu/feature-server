get:
  summary: Returns the dataource by it unique id
  operationId: datasource
  responses:
    '200':
      description: A datasource
      content:
        application/json:
          schema:
            $ref: '../schemas/datasource.yaml'
parameters:
- name: id
  in: path
  required: true
  schema:
    type: number
    format: int32
